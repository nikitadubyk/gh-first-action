name: First Action
on: workflow_dispatch
jobs:
  fisrt-job:
    name: deploy and staging
    runs-on: ubuntu-latest
    steps: 
      - name: Print greeting
        run: echo "Hello world"
      - name: Print goodbye
        run: echo "Goodbye"
